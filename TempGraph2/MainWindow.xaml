<Window x:Class="TempGraph2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:TempGraph2"
        mc:Ignorable="d"
        Title="CPU and GPU Temperatures" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <lvc:CartesianChart x:Name="cartesianChart" Series="{Binding Temps}" Margin="0,0,0,43">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Time (Seconds)" />
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>

        <Slider x:Name="timeWindowSlider" Minimum="1" Maximum="60" Value="0" 
                ValueChanged="TimeWindowSlider_ValueChanged" VerticalAlignment="Top" Margin="0,408,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="323,370,302,12">
            <Label Content="Time Window (Seconds):" VerticalAlignment="Center"/>
            <TextBox x:Name="timeWindowTextBox" Text="0" 
             LostFocus="TimeWindowTextBox_LostFocus" VerticalAlignment="Center"
             Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>